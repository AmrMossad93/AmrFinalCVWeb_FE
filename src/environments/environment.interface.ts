/**
 * Application environment configuration.
 * Use fileReplacements in angular.json to load the correct file per build configuration.
 */
export interface Environment {
  production: boolean;
  formSpreeId: string;
}
